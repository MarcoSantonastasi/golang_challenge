version: "3.8"

volumes:
  postgres-data:

services:
  db:
    image: postgres:latest
    volumes:
      - postgres-data:/var/lib/postgresql/data
    env_file:
      - .env
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
